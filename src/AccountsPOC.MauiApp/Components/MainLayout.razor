@inherits LayoutComponentBase

<div class="page">
    <div class="content">
        @Body
    </div>
    <div class="nav-bar">
        <NavLink href="/" Match="NavLinkMatch.All">
            <span>🏠 Home</span>
        </NavLink>
        <NavLink href="/stock-check">
            <span>📦 Stock Check</span>
        </NavLink>
        <NavLink href="/pick-lists">
            <span>📋 Pick Lists</span>
        </NavLink>
        <NavLink href="/delivery-routes">
            <span>🚚 Routes</span>
        </NavLink>
        <NavLink href="/stock-counts">
            <span>📊 Stock Count</span>
        </NavLink>
    </div>
</div>

<style>
    .page {
        display: flex;
        flex-direction: column;
        height: 100vh;
    }

    .content {
        flex: 1;
        overflow-y: auto;
        padding: 16px;
    }

    .nav-bar {
        display: flex;
        justify-content: space-around;
        background-color: #343a40;
        padding: 8px 0;
        box-shadow: 0 -2px 5px rgba(0,0,0,0.1);
    }

    .nav-bar a {
        color: white;
        text-decoration: none;
        padding: 8px 16px;
        border-radius: 8px;
        font-size: 0.875rem;
        text-align: center;
        display: flex;
        flex-direction: column;
        align-items: center;
    }

    .nav-bar a:hover,
    .nav-bar a.active {
        background-color: #495057;
    }

    .nav-bar span {
        font-size: 0.75rem;
    }
</style>
