{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "NOTE": "For production deployment, store JWT:Key in environment variables or Azure Key Vault. Generate a secure random key of at least 256 bits (32 bytes)."
}
